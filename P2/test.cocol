COMPILER Test

CHARACTERS

letter = "abcdefghijk".
digit = "0123456789".
hexdigit = digit + "ABCDEF".
tab = CHR(9).
eol = CHR(10).
algo = "+"+"-".
operadores = "+-=()[]{}|.<>".
token=digit{tab}.

KEYWORDS

if="if"
while="while"

TOKENS

id = letter{letter} EXCEPT KEYWORDS.
number = digit{digit}.
hexnumber = hexdigit{hexdigit}"(H)".

PRODUCTIONS

END Test